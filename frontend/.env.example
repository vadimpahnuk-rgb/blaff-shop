# Frontend environment (Vite). Copy to .env.local for local dev.
# Baked into the build at compile time — set per-environment in Vercel for deploys.

# Backend API base URL (must include the /api suffix).
# Local default if unset: http://localhost:3001/api
VITE_API_URL=http://localhost:3001/api
